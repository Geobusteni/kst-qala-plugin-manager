/**
 * PostCSS configuration for Qala Plugin Manager
 *
 * @wordpress/scripts includes PostCSS by default.
 * This file overrides the parent directory config.
 */

module.exports = {
	plugins: [
		// No additional plugins needed - @wordpress/scripts handles everything
	],
};
